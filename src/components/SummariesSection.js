import React from 'react';

const SummariesSection = () => {
  return (
    <section>
      <h2>Your Past Summaries</h2>
      {/* Replace with logic to display past summaries (placeholder) */}
      <p>No summaries yet. Summarize your meetings below!</p>
    </section>
  );
};

export default SummariesSection;
