import React from 'react';

const TranscriptToSummarize = () => {
  return (
    <section>
      <h2>Summarize Existing Transcript</h2>
      {/* Replace with functionality to paste/upload transcript (placeholder) */}
      <textarea placeholder="Paste your transcript here"></textarea>
      <button disabled>Summarize</button>
    </section>
  );
};

export default TranscriptToSummarize;
