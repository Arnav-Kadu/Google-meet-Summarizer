import React from 'react';

const UploadSection = () => {
  return (
    <section>
      <h2>Summarize a Google Meet Recording</h2>
      {/* Replace with functionality to select and upload recording (placeholder) */}
      <input type="file" disabled />
      <button disabled>Summarize</button>
    </section>
  );
};

export default UploadSection;
